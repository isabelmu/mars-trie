use history::State;
use history::History;

struct Nav {
    state_: State
}

/*
impl Nav {
    fn has_child() -> bool;
    fn next_sibling() -> bool;
    fn up() -> bool;
    fn is_terminal() -> bool;
    fn get_string() -> &str;
    fn is_end() -> bool
}
*/

